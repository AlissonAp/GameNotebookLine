<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.testeconsumerestapi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="The Notbook Line"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".initialPageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".loginUserActivity"></activity>
        <activity android:name=".createUserActivity"></activity>
        <activity android:name=".listMissaoActivity"></activity>
        <activity android:name=".startMissao"></activity>
        <activity android:name=".stepsSelecaoPecasMissao"></activity>
        <activity android:label="@string/app_name"  android:name=".infoUsuario"></activity>
        <activity android:name=".finishMission"></activity>

    </application>

</manifest>